 :80 {
    reverse_proxy web:3000

    # ログを有効化（デバッグに便利）
    log

    # ヘッダー圧縮など本番向け設定
    encode zstd gzip
}

# 将来ドメイン取得後に使うブロック
# yourdomain.com {
#     reverse_proxy web:3000
#     tls your-email@example.com
# }

